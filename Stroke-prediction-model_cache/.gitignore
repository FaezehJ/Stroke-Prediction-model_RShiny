# --- R / RStudio ---
.Rproj.user/
.Rhistory
.RData
.Ruserdata
.Rapp.history

# Project / env
.Rprofile
.Renviron

# renv (the renv folder itself is tracked; the libs are ignored by renv’s own .gitignore)
renv/staging/
renv/python/
renv/lock/      # just in case

# RStudio project files (keep the .Rproj if you want)
*.Rproj

# Shiny deployment artifacts (Posit Connect / shinyapps.io)
rsconnect/

# Knitted / rendered outputs (keep sources, ignore heavy HTML)
*.html
*.nb.html

# Local data & outputs (keep only metadata/readme)
data/*
!data/README.md
!data/example*.csv   # allow small synthetic examples if you add them

# Common export files
*.csv
*.tsv
*.rds
*.rda
predictions_*.csv

# OS junk
.DS_Store
Thumbs.db
Icon?


# --- Project / Shiny specific additions ---

# R project/user config
.Rprofile

# Knitted / rendered (keep sources, ignore heavy HTML)
*.html
*.nb.html

# Local data & outputs (keep only metadata/readme)
data/*
!data/README.md
!data/example*.csv   # allow tiny synthetic examples if you add them

# Prediction exports and serialized objects
predictions_*.csv
*.csv
*.tsv
*.rds
*.rda

# YAML: ignore general YAML, but keep common project configs & CI
*.yml
*.yaml
!_quarto.yml
!_site.yml
!_bookdown.yml
!_output.yml
!.github/workflows/*.yml

# OS junk
.DS_Store
Thumbs.db
Icon?

